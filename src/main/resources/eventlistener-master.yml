apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: {{ .Values.cluster }}-selenium-GROUPS-URL-CLEAN_RELEASE_BRANCH-cronjob
  namespace: tester-pipelines
spec:
  namespaceSelector: {}
  resources: {}
  serviceAccountName: pipeline
  triggers:
    - bindings:
        - kind: TriggerBinding
          ref: {{ .Values.cluster }}-selenium-GROUPS-URL-CLEAN_RELEASE_BRANCH-cronjob-binding
      name: {{ .Values.cluster }}-selenium-GROUPS-URL-CLEAN_RELEASE_BRANCH-cronjob-trigger
      template:
        ref: {{ .Values.cluster }}-selenium-GROUPS-URL-CLEAN_RELEASE_BRANCH-cronjob-tt